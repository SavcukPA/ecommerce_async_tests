[pytest]
log_format = %(asctime)s.%(msecs)03d %(name)s:%(lineno)d %(levelname)s - %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
markers =
    ui: тесты web


python_files =
    tests_*.py
    *_tests.py

python_functions =
    tests_*

python_classes =
    Tests*